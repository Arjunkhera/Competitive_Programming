public class Test {
    public static void main(String[] args) {
        String empty = "";
        System.out.println(empty.length());
        Character ch = 'c';
        Character.isDigit(ch);
    }
}
